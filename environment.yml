name: nexchem_env
channels:
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.24
  - pandas>=2.0
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - seaborn>=0.12
  - openpyxl>=3.1
  - scipy>=1.11
  - streamlit>=1.28
  - pip
  - pip:
      - ramanspy
      - plotly==6.6.0
      - pypdf
      # pip install --no-build-isolation git+https://github.com/rohanisaac/spc.git
      # install spc separately 